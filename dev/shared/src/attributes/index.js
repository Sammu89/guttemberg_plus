/**
 * Shared Attributes - Index
 *
 * Simplified - all attribute definitions now come from schema-generated files.
 * Schema (schemas/*.json) is the single source of truth.
 *
 * @package
 * @since 1.0.0
 */

export { getAllDefaults } from './attribute-defaults';
